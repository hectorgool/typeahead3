
curl -XGET 'localhost:9200/geonames2/geoname/_mapping?pretty=true'

curl -XGET 'localhost:9200/geonames2/geoname/_search?pretty=true'

curl -XGET "localhost:9200/geonames2/geoname/_search" -d '
{
    "query": {
        "term": {
            "name.autocomplete": "l"
        }
    },
    "facets": {
        "name": {
            "terms": {
                "field": "name"
            }
        }
    }
}
'

curl -XGET "localhost:9200/geonames2/geoname/_search" -d '
{
    "query": {
        "term": {
            "name.autocomplete": "las"
        }
    },
    "facets": {
        "name": {
            "terms": {
                "field": "name"
            }
        }
    }
}
'



curl -XGET "localhost:9200/geonames2/geoname/_search" -d '
{
    "query": {
        "term": {
            "name.autocomplete": "Las Pal"
        }
    },
    "facets": {
        "name": {
            "terms": {
                "field": "name"
            }
        }
    }
}
'